spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/food_app
    username: alex
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081